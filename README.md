# eslint-plugin-jest-set

## Usage

```
npm install --save-dev eslint-plugin-jest-set
```

In your .eslintrc:

```json
{
  "plugins": ["jest-set"],
  "rules": {
    "no-undef": 0,
    "jest-set/no-undef": "error"
  }
}
```
